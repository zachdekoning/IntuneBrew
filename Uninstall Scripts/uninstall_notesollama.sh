#!/bin/bash
# Uninstall script for NotesOllama
# Generated by IntuneBrew

# Exit on error
set -e

echo "Uninstalling NotesOllama..."

# Check if running as root
if [ "$EUID" -ne 0 ]; then
  echo "Please run as root"
  exit 1
fi

# Kill application process if running
echo "Stopping NotesOllama if running..."
pkill -f "NotesOllama" 2>/dev/null || true

# Kill application with bundle ID NotesOllama if running
echo "Stopping application with bundle ID NotesOllama if running..."
killall -9 "NotesOllama" 2>/dev/null || true

# Remove /Applications/NotesOllama.app
echo "Removing /Applications/NotesOllama.app..."
if [ -d "/Applications/NotesOllama.app" ]; then
    rm -rf "/Applications/NotesOllama.app" 2>/dev/null || true
elif [ -f "/Applications/NotesOllama.app" ]; then
    rm -f "/Applications/NotesOllama.app" 2>/dev/null || true
fi

# Remove $HOME/Library/Caches/app.smallest.NotesOllama
echo "Removing $HOME/Library/Caches/app.smallest.NotesOllama..."
if [ -d "$HOME/Library/Caches/app.smallest.NotesOllama" ]; then
    rm -rf "$HOME/Library/Caches/app.smallest.NotesOllama" 2>/dev/null || true
elif [ -f "$HOME/Library/Caches/app.smallest.NotesOllama" ]; then
    rm -f "$HOME/Library/Caches/app.smallest.NotesOllama" 2>/dev/null || true
fi

# Remove $HOME/Library/HTTPStorages/app.smallest.NotesOllama
echo "Removing $HOME/Library/HTTPStorages/app.smallest.NotesOllama..."
if [ -d "$HOME/Library/HTTPStorages/app.smallest.NotesOllama" ]; then
    rm -rf "$HOME/Library/HTTPStorages/app.smallest.NotesOllama" 2>/dev/null || true
elif [ -f "$HOME/Library/HTTPStorages/app.smallest.NotesOllama" ]; then
    rm -f "$HOME/Library/HTTPStorages/app.smallest.NotesOllama" 2>/dev/null || true
fi

echo "Uninstallation complete!"
exit 0
