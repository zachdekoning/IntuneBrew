#!/bin/bash
# Uninstall script for ChatWise
# Generated by IntuneBrew

# Exit on error
set -e

echo "Uninstalling ChatWise..."

# Check if running as root
if [ "$EUID" -ne 0 ]; then
  echo "Please run as root"
  exit 1
fi

# Kill application process if running
echo "Stopping ChatWise if running..."
pkill -f "ChatWise" 2>/dev/null || true

# Kill application with bundle ID app.chatwise if running
echo "Stopping application with bundle ID app.chatwise if running..."
killall -9 "app.chatwise" 2>/dev/null || true

# Remove /Applications/ChatWise.app
echo "Removing /Applications/ChatWise.app..."
if [ -d "/Applications/ChatWise.app" ]; then
    rm -rf "/Applications/ChatWise.app" 2>/dev/null || true
elif [ -f "/Applications/ChatWise.app" ]; then
    rm -f "/Applications/ChatWise.app" 2>/dev/null || true
fi

# Remove $HOME/Library/Application Support/app.chatwise
echo "Removing $HOME/Library/Application Support/app.chatwise..."
if [ -d "$HOME/Library/Application Support/app.chatwise" ]; then
    rm -rf "$HOME/Library/Application Support/app.chatwise" 2>/dev/null || true
elif [ -f "$HOME/Library/Application Support/app.chatwise" ]; then
    rm -f "$HOME/Library/Application Support/app.chatwise" 2>/dev/null || true
fi

# Remove $HOME/Library/Caches/app.chatwise
echo "Removing $HOME/Library/Caches/app.chatwise..."
if [ -d "$HOME/Library/Caches/app.chatwise" ]; then
    rm -rf "$HOME/Library/Caches/app.chatwise" 2>/dev/null || true
elif [ -f "$HOME/Library/Caches/app.chatwise" ]; then
    rm -f "$HOME/Library/Caches/app.chatwise" 2>/dev/null || true
fi

# Remove $HOME/Library/Saved Application State/app.chatwise.savedState
echo "Removing $HOME/Library/Saved Application State/app.chatwise.savedState..."
if [ -d "$HOME/Library/Saved Application State/app.chatwise.savedState" ]; then
    rm -rf "$HOME/Library/Saved Application State/app.chatwise.savedState" 2>/dev/null || true
elif [ -f "$HOME/Library/Saved Application State/app.chatwise.savedState" ]; then
    rm -f "$HOME/Library/Saved Application State/app.chatwise.savedState" 2>/dev/null || true
fi

# Remove $HOME/Library/WebKit/app.chatwise
echo "Removing $HOME/Library/WebKit/app.chatwise..."
if [ -d "$HOME/Library/WebKit/app.chatwise" ]; then
    rm -rf "$HOME/Library/WebKit/app.chatwise" 2>/dev/null || true
elif [ -f "$HOME/Library/WebKit/app.chatwise" ]; then
    rm -f "$HOME/Library/WebKit/app.chatwise" 2>/dev/null || true
fi

echo "Uninstallation complete!"
exit 0
